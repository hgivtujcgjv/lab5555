name: CI


on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: 1
        run: |
          sudo apt-get update
          sudo apt-get install -y libboost-test-dev
      - name: 2
        run: cmake -H. -B_build -DBUILD_TESTS=ON
      - name: 3
        run: cmake --build _build
      - name: 4
        run: cmake --build _build --target test -- ARGS=--verbose
      - name: 6
        run: |
          pip install cpp-coveralls
          export COVERALLS_REPO_TOKEN=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
          coveralls --exclude lib --exclude tests --gcov-options '\-lp'
      - name: 7
        uses: coverallsapp/github-action@v1.1.2
        with:
          github-token: ${{ github.token }}
